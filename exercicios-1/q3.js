/* Conteúdos abordados: operadores aritméticos

Vamos fazer algumas operações simples.
Queremos imprimir a área e o perímetro de um retângulo, sabemos que
a base do retângulo é 5 e a altura é 8.

Escreva em frases curtas o que será necessário fazer, para imprimir o valor
da área e perímetro desse retângulo.
Escreva o código para ser possível ver a impressão.
*/

let base = 5;
    altura = 8;
    perimetro = 2*(base*altura);
    area = base*altura;

console.log(area);
console.log(perimetro);

// primeiro, é necessario realizar os calculos, usei as variaveis base e altura para armazenar os valores
// depois criei uma novas variaveis para calcular area e perimetro
// nas variáveis, apliquei as formulas
// depois usei o console.log() para exibir os valores
